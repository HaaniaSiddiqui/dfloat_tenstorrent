# dfloat_tenstorrent


## Project Structure:

- experiments/
  - coding_compare.py → compression analysis
  - export_grouped_format.py → creates grouped artifacts
  - execution.py → reload + execute grouped artifacts
  - grouped_runtime_and_format.py → grouped validation

- core/
  - utils.py → shared helper functions

- artifacts/
  - grouped compressed model format (used for execution)
 

 
